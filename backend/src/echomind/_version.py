"""Versione del package EchoMind.

Single source of truth per la versione. `pyproject.toml` legge da qui in
futuro (via hatch dynamic versioning); per ora la teniamo allineata a mano.
"""

__version__ = "0.0.0"
