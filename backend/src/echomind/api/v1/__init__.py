"""API v1: prima versione del contratto pubblico.

Convenzione: tutti gli endpoint di v1 vivono sotto `/api/v1/...`.
Quando in futuro faremo breaking change, useremo `/api/v2/` mantenendo
v1 funzionante in parallelo per il periodo di transizione.
"""
